{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create client.zip",
            "type": "shell",
            "command": "zip",
            "args": [
                "-u",
                "-r",
                "client.zip",
                "${workspaceFolder}/client"
            ],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/client"
            },
        }
    ]
}